$('#newConModal').foundation("reveal", "close");
$('#conventionList').html("<%= escape_javascript(render 'list') %>");
$('#flash-messages').html("<%= escape_javascript(render 'flash') %>");
$(document).foundation();
